extends layout

block content
    .container
        h1 User Detail
        ul
            li ID: #{user.id}
            li Name: #{user.firstName} #{user.lastName}
            li Company: #{user.company}
            li Email: #{user.email}